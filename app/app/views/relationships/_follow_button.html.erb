<% if current_user.following?(user) %>
  <%= link_to "フォロー中", user_relationship_path(user.id), class: "btn btn-primary edit-profile-btn", method: :DELETE %>
<% else %>
  <%= link_to "フォローする", user_relationships_path(user.id), class: "btn btn-dark edit-profile-btn", method: :POST %>
<% end %>