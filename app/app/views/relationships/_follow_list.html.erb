<% @user.relationships.each do |user| %>
  <table>
    <thead>
      <tr>
        <th>name</th>
        <th></th>
        <th></th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td><%= @user.name %></td>
        <td>フォロー: <%= @user.followings.count %></td>
        <td>フォロワー: <%= @user.followers.count %></td>
      </tr>
    </tbody>
  </table>
<% end %>